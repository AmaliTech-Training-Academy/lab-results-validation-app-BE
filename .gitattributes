# Shell scripts must keep LF endings: they run on Linux (CI runners, the dev box, git hooks),
# and CRLF makes the interpreter fail with "bad interpreter: /usr/bin/env bash^M".
*.sh text eol=lf
mvnw text eol=lf
