# Template for local environment variables.
# Copy to .env (which is gitignored) and fill in the values:
#   cp .env.example .env

# --- Database ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=labresults
DB_USER=labresults
DB_PASSWORD=


JWT_SECRET=hJJ/kGaAiXnUK/Kuzrp/ViWaw7UOK1oPe/tNprujUNg=

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379

# --- Mail (SMTP) ---
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=

# --- URLs ---
FRONTEND_URL=http://localhost:5173
BASE_URL=http://localhost:8080
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# --- AWS S3 (SharePoint file store) ---
AWS_REGION=eu-west-1
S3_BUCKET=lab-results-validator-bucket
# Leave blank locally to fall back to the default AWS credentials chain (aws configure).
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
