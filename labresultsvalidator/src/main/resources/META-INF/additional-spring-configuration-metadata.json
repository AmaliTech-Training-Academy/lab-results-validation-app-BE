{
  "properties": [
    {
      "name": "app.frontend-url",
      "type": "java.lang.String",
      "description": "Base URL of the frontend application, used to build links in emails (e.g. password reset links). Overridden at runtime by the FRONTEND_URL environment variable.",
      "defaultValue": "http://localhost:3000"
    }
  ]
}
